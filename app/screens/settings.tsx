import { Text, View } from "react-native";

const Settings = () => {
  return (
    <View style={{ paddingTop: 50, paddingHorizontal: 26 }}>
      <Text>Settings</Text>
    </View>
  );
};

export default Settings;
